import { Title } from '@angular/platform-browser';
export interface Room {
    id?: number,
    numberRoom?: string,
    priceNeight?: number,
    description?: string,
    state?: boolean,
    imgRoom?: string,
    typeRoomId?: number
}
